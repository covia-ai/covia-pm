{
  "name": "pm:fetchReadSummary",
  "description": "Fetch a meeting report and insights from Read.ai via MCP.",
  "creator": "Covia PM",
  "dateCreated": "2026-02-21T00:00:00Z",
  "keywords": ["read", "report", "meeting-intelligence", "mcp"],
  "operation": {
    "adapter": "orchestrator",
    "input": {
      "type": "object",
      "properties": {
        "callRef": { "type": "string", "description": "Meeting ID or URL" },
        "server": { "type": "string", "description": "MCP server URL" },
        "token": { "type": "string", "secret": true }
      },
      "required": ["callRef", "server"]
    },
    "output": {
      "type": "object",
      "properties": {
        "transcript": { "type": "string" },
        "summary": { "type": "string" }
      }
    },
    "steps": [
      {
        "op": "mcp:tools:call",
        "name": "Fetch Read.ai Report",
        "input": {
          "server": ["input", "server"],
          "toolName": ["const", "get_report"],
          "arguments": { "meetingId": ["input", "callRef"] },
          "token": ["input", "token"]
        }
      }
    ],
    "result": {
      "transcript": [0, "transcript"],
      "summary": [0, "summary"]
    }
  }
}
