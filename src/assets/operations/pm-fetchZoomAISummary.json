{
  "name": "pm:fetchZoomAISummary",
  "description": "Fetch a Zoom AI Companion meeting summary via MCP.",
  "creator": "Covia PM",
  "dateCreated": "2026-02-21T00:00:00Z",
  "keywords": ["zoom", "summary", "meeting-intelligence", "mcp"],
  "operation": {
    "adapter": "orchestrator",
    "input": {
      "type": "object",
      "properties": {
        "callRef": { "type": "string", "description": "Meeting ID or URL" },
        "server": { "type": "string", "description": "MCP server URL" },
        "token": { "type": "string", "secret": true }
      },
      "required": ["callRef", "server"]
    },
    "output": {
      "type": "object",
      "properties": {
        "transcript": { "type": "string" },
        "summary": { "type": "string" }
      }
    },
    "steps": [
      {
        "op": "mcp:tools:call",
        "name": "Fetch Zoom AI Summary",
        "input": {
          "server": ["input", "server"],
          "toolName": ["const", "get_meeting_summary"],
          "arguments": { "meetingId": ["input", "callRef"] },
          "token": ["input", "token"]
        }
      }
    ],
    "result": {
      "transcript": [0, "transcript"],
      "summary": [0, "summary"]
    }
  }
}
